spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: postgres
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
  jpa:
    mapping-resources:
      - hbm/film-repository.hbm.xml
      - hbm/user-repository.hbm.xml
    open-in-view: false
